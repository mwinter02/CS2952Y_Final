This directory is a placeholder for the nlohmann/json single-header library.

To use the JSON loader in Mesh.cpp, add `json.hpp` from https://github.com/nlohmann/json
into this folder and update the include path in CMake if necessary.

